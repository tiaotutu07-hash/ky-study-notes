# CO 第3章 · 存储器层次结构 · 知识树

## (一) 存储器的分类
- 本质:
- 关键参数:
- 🔴 红坑:
- 🔗 跨章关联:
- 进度: ⬜ 未开始

## (二) 层次化存储器的基本结构
- 本质:
- 关键参数:
- 🔴 红坑:
- 🔗 跨章关联:
- 进度: ⬜ 未开始

## (三) 半导体随机存取存储器

### (三)-1 SRAM存储器
- 本质:
- 关键参数:
- 🔴 红坑:
- 🔗 跨章关联:
- 进度: ⬜ 未开始

### (三)-2 DRAM存储器
- 本质:
- 关键参数:
- 🔴 红坑:
- 🔗 跨章关联:
- 进度: ⬜ 未开始

### (三)-3 Flash存储器
- 本质:
- 关键参数:
- 🔴 红坑:
- 🔗 跨章关联:
- 进度: ⬜ 未开始

## (四) 主存储器

### (四)-1 DRAM芯片和内存条
- 本质:
- 关键参数:
- 🔴 红坑:
- 🔗 跨章关联:
- 进度: ⬜ 未开始

### (四)-2 多模块存储器
- 本质:
- 关键参数:
- 🔴 红坑:
- 🔗 跨章关联:
- 进度: ⬜ 未开始

### (四)-3 主存和CPU之间的连接
- 本质:
- 关键参数:
- 🔴 红坑:
- 🔗 跨章关联:
- 进度: ⬜ 未开始

## (五) 外部存储器

### (五)-1 磁盘存储器
- 本质:
- 关键参数:
- 🔴 红坑:
- 🔗 跨章关联:
- 进度: ⬜ 未开始

### (五)-2 固态硬盘(SSD)
- 本质:
- 关键参数:
- 🔴 红坑:
- 🔗 跨章关联:
- 进度: ⬜ 未开始

## (六) 高速缓冲存储器(Cache)

### (六)-1 Cache的基本原理
- 本质: SRAM 在 CPU 和主存之间做"热数据缓冲", 靠程序局部性原理实现 95%+ 命中率, 弥合速度鸿沟
- 关键参数:
  - 时间局部性 + 空间局部性 + 工作集(当下反复访问的小撮地址)
  - 命中率 H = Nc/(Nc+Nm)
  - 平均访问时间 T_a = T_c + (1-H)·T_m (串行版,408 默认)
  - 行/块/组 严格定义(CSAPP): 块=信息包(数据), 行=容器(数据+辅助位), 组=多行集合
  - 辅助位: V(1) + Tag(t) + (脏位 if 回写) + (LRU位 log₂E if LRU)
  - Cache 数据区容量 C = S × E × B, 不含辅助位
- 🔴 红坑:
  - 行/块/组混用(组相联场景必须严格区分)
  - Ta 公式有串/并行两版, 408 默认串行
  - CPU 用主存物理地址访问 Cache, 不存在"Cache 地址"
  - 多核环境下写策略不解决一致性, 要 MESI 协议(🟣 真题盲区)
  - 多级 Cache 全局命中率 = H₁+(1−H₁)·H₂, 非简单相加
  - 不命中三分类(冷/冲突/容量)——CSAPP 补充
- 🔗 跨章关联:
  - Cache 由 SRAM 组成 → 第3章(一) 存储分类
  - "上层缓存下层" → 第3章(二) 层次化存储
  - 与虚存的对比: Cache 解速度, 虚存解容量 → 第3章(七)
- 进度: ✅

### (六)-2 Cache和主存之间的映射方式
- 本质: 主存块能放在 Cache 哪些位置的"自由度", 三种方式本质是 (S,E,B,m) 中 E(每组行数) 的取值差异
- 关键参数:
  - 通用组织 (S,E,B,m): m=地址位, B=2^b 块大小, S=2^s 组数, E=路数
  - 直接映射 E=1, 地址 |Tag(t)|行号(c=s)|偏移(b)|, 1 个比较器
  - 全相联 S=1, 地址 |Tag(t)|偏移(b)| (无组索引!), N 个比较器
  - 组相联 1<E<C/B, 地址 |Tag(t)|组号(s)|偏移(b)|, E 个比较器
  - 行号/组号 = 主存块号 mod 行数/组数
  - Tag 位 t = m − s − b
  - 符号桥接: 王道 c = CSAPP s(直接映射场景)
- 🔴 红坑:
  - 组索引位永远在中间(高位致连续块全冲突)
  - 全相联无组索引位(根本没有, 不是填 0)
  - 比较器数 = 组内行数 E(不是总行数)
  - 退化关系: S=1→全相联, E=1→直接映射
  - 直接映射不需要替换算法(目标行唯一)
  - 按字编址 vs 字节编址: b = log₂(块内编址单元数)(🟣 真题盲区, 2010/2013/2020 真题踩过)
- 🔗 跨章关联:
  - 全相联思想 → 第3章(七) TLB(按内容寻址)
  - r 路组相联是主流(Core i7 L1/L2 为 8 路, L3 为 16 路)
  - 相联存储器 → 硬件实现共享
- 进度: ✅

### (六)-3 Cache中主存块的替换算法
- 本质: 组相联/全相联中"组满了换谁出去", 核心差异在是否利用局部性
- 关键参数:
  - 四种: RAND/FIFO/LRU/LFU
  - LRU 最考查, 利用时间局部性
  - LRU 位数 = log₂(组内 E): 2路=1位, 4路=2位, 8路=3位
  - LRU 计数三规则:
    - 命中: 被命中行清零 + 比原值小的全部+1(比原值大的不变!)
    - 缺失+有空闲: 新行清零 + 其他非空闲行+1
    - 缺失+无空闲: 替换最大计数行 + 新行清零 + 其余+1
  - 抖动: 工作集 > E 时命中率可能为 0
- 🔴 红坑:
  - 直接映射不需要替换算法
  - LRU 关注"最近", LFU 关注"总数", 别混
  - 抖动可用数组填充修复(CSAPP)
  - LRU 位数按"组内 E", 不按总行数
  - 🟣 Bélády 异常: FIFO 增加缓存可能反而降低命中率, LRU/RAND 不会
  - 命中规则: 只对"比原值小的"+1, 比原值大的不动
- 🔗 跨章关联:
  - LRU 思想 → 第3章(七) 虚存页替换
  - Bélády 异常 → 第3章(七) 缺页同样存在
  - 抖动概念 → 第3章(七) 缺页抖动
- 进度: ✅

### (六)-4 Cache写策略
- 本质: "命中时主存要不要同步" + "不命中时要不要调块" 两个轴的组合, 现实只用两种搭配
- 关键参数:
  - 写命中: 全写法(直写, Write Through) / 回写法(Write Back)
  - 写不命中: 写分配法(Write Allocate) / 非写分配法
  - 搭配1: 全写+非写分配 (配写缓冲, 安全优先)
  - 搭配2: 回写+写分配 (配脏位, 性能优先)
  - 现代 CPU L1 d-cache 主流: 回写+写分配
  - 写缓冲(write buffer): 小队列, CPU 写完即返回, 异步写主存
- 🔴 红坑:
  - 脏位仅回写法需要, 全写法无脏位
  - 反向搭配几乎不考
  - 写缓冲在高频写时可能溢出
  - 直写法 = 全写法 = Write Through(三个名字一回事)
  - 🟣 多核一致性: 写策略只解决 Cache↔主存, 核间多副本要 MESI 协议
- 🔗 跨章关联:
  - 写回思想 → 第3章(七) 虚存页换出(脏页才写回)
  - 多核一致性 → 第5章末/超纲 MESI 协议
- 进度: ✅

## (七) 虚拟存储器

### (七)-1 虚拟存储器的基本概念
- 本质:
- 关键参数:
- 🔴 红坑:
- 🔗 跨章关联:
- 进度: ⬜ 未开始

### (七)-2 页式虚拟存储器(含页表、地址转换、TLB)
- 本质:
- 关键参数:
- 🔴 红坑:
- 🔗 跨章关联:
- 进度: ⬜ 未开始

### (七)-3 段式虚拟存储器
- 本质:
- 关键参数:
- 🔴 红坑:
- 🔗 跨章关联:
- 进度: ⬜ 未开始

### (七)-4 段页式虚拟存储器
- 本质:
- 关键参数:
- 🔴 红坑:
- 🔗 跨章关联:
- 进度: ⬜ 未开始
