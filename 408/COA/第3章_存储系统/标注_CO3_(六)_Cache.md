# 框架图标注清单 · CO3 (六) 高速缓冲存储器 (Cache) · v6.2

> 来源 study.md v6.2;蓝笔每条 ≤15字,红笔每条 ≤20字。
> 任逸拿着这份清单在物理框架图上标注。

---

## 🔵 蓝笔标注(知识点/规律)  共 25 条

### 基本原理(7 条)

1. 标在 [Cache] 节点旁 → "SRAM 组成,集成在 CPU"
2. 标在 [局部性原理] 旁 → "时间=刚访问会再访问"
3. 标在 [局部性原理] 旁 → "空间=邻近会被访问"
4. 标在 [行/块/组] 旁 → "块=信息包,行=容器,组=多行"
5. 标在 [辅助位] 旁 → "V/Tag 必备;脏位仅回写"
6. 标在 [辅助位] 旁 → "LRU 位 = log₂E,按组内路数"
7. 标在 [命中率] 旁 → "Ta = Tc + (1-H)·Tm 默认串行"

### 映射方式(7 条)

8. 标在 [三种映射] 总节点旁 → "本质 = E 取值不同"
9. 标在 [(S,E,B,m)] 旁 → "C=S·E·B,t=m-s-b"
10. 标在 [直接映射] 旁 → "E=1,1 个比较器"
11. 标在 [全相联] 旁 → "S=1,无组索引位"
12. 标在 [组相联] 旁 → "1<E<C/B,E 个比较器"
13. 标在 [地址结构] 旁 → "组索引永远在中间位"
14. 标在 [退化关系] 旁 → "Q=1全相联,r=1直接"

### 替换算法(5 条)

15. 标在 [替换算法] 总节点旁 → "直接映射无需替换"
16. 标在 [LRU] 旁 → "命中清0,比小的全+1"
17. 标在 [LRU] 旁 → "缺失无空闲:换最大,新0,其余+1"
18. 标在 [LRU] 旁 → "比原值大的不动 ⚠️"
19. 标在 [抖动] 旁 → "工作集>路数时命中率=0"

### 写策略(4 条)

20. 标在 [写命中] 旁 → "全写=直写=Write Through"
21. 标在 [回写法] 旁 → "需脏位,替换时才写"
22. 标在 [写策略搭配] 旁 → "全写+非分配 / 回写+分配"
23. 标在 [写策略搭配] 旁 → "现代 L1d 主流:回写+分配"

### 容量+应用+新考点(2 条)

24. 标在 [容量计算] 旁 → "总=行数×(数据+辅助位)"
25. 标在 [多级 Cache] 旁 → "全局率=H₁+(1-H₁)·H₂"

---

## 🔴 红笔标注(易错/陷阱)  共 18 条

1. 标在 [行/块/组] 旁 → "⚠️ 组相联场景必严分"
2. 标在 [辅助位] 旁 → "⚠️ 脏位仅回写,直写无脏位"
3. 标在 [LRU 位] 旁 → "⚠️ 按组内路数,不按总行数"
4. 标在 [组索引位置] 旁 → "⚠️ 高位致连续块全冲突"
5. 标在 [全相联地址] 旁 → "⚠️ 根本无组索引字段"
6. 标在 [比较器数量] 旁 → "⚠️ =组内行数,非总行数"
7. 标在 [直接映射替换] 旁 → "⚠️ 不需算法,直接覆盖"
8. 标在 [抖动] 旁 → "⚠️ 工作集>E 时命中率 0%"
9. 标在 [写策略搭配] 旁 → "⚠️ 反向搭配几乎不考"
10. 标在 [Cache 容量] 旁 → "⚠️ 数据区vs总容量两口径"
11. 标在 [Ta 公式] 旁 → "⚠️ 408默认串行,并行罕见"
12. 标在 [多级 Cache] 旁 → "⚠️ 全局率非简单相加"
13. 标在 [Cache访问] 旁 → "⚠️ 用物理地址,无Cache地址"
14. 标在 [LRU 命中规则] 旁 → "⚠️ 仅比原值小的+1"
15. 标在 [编址方式] 旁 → "⚠️ 按字编址 b=log₂块字数"
16. 标在 [多核] 旁 → "⚠️ 写策略不解多核一致性"
17. 标在 [MESI] 旁 → "⚠️ 多核要专门协议(MESI)"
18. 标在 [FIFO] 旁 → "⚠️ Bélády:扩容反降命中率"

---

## ↔️ 跨章关联标注  共 5 条

1. 标在 [Cache] 节点旁 → "→第3章(一)·SRAM 组成"
2. 标在 [Cache] 节点旁 → "→第3章(二)·层次化存储典型"
3. 标在 [全相联] 旁 → "→第3章(七)·TLB 同款相联"
4. 标在 [LRU/抖动] 旁 → "→第3章(七)·虚存页替换/缺页"
5. 标在 [分离 Cache] 旁 → "→第5章·流水线避结构冒险"

---

## 📊 标注总数

- 🔵 蓝笔 25 条
- 🔴 红笔 18 条
- ↔️ 跨章 5 条
- **合计 48 条**(对应默写 48 题)

## v6 → v6.2 标注差异

| 类别 | v6 | v6.2 | 新增 |
|---|---|---|---|
| 蓝笔 | 23 | 25 | +(S,E,B,m) 框架 / 比较器规律 |
| 红笔 | 13 | 18 | +按字编址 / MESI / Bélády / LRU命中细则 / 多核写策略 |
| 跨章 | 5 | 5 | (持平,LRU/抖动合并指向虚存) |
