# 框架图标注清单 · 模块 2.1 数制与编码

> 任逸拿这份清单照着标到物理框架图上即可。
> **蓝笔写知识点/规律(≤15 字),红笔写易错/陷阱(≤20 字),跨章关联另起标签。**

---

## 🔵 蓝笔标注(知识点/规律)  共 20 条

1. 标在 **「进位计数制」** 旁 → "基数 r,逢 r 进 1"
2. 标在 **「位权」** 旁 → "权 = r^i,远小数点权大"
3. 标在 **「二↔八/十六」** 旁 → "3 位/4 位分组直接换"
4. 标在 **「十→任意」** 旁 → "整除基取余,小乘基取整"
5. 标在 **「ASCII」** 旁 → "'0'=48 'A'=65 'a'=97"
6. 标在 **「大端小端」** 旁 → "大端:高字节→低地址"
7. 标在 **「真值/机器数」** 旁 → "真值=人写,机器数=0/1 串"
8. 标在 **「原码」** 旁 → "符号位 + 真值绝对值"
9. 标在 **「原码范围」** 旁 → "-(2^n-1) ~ 2^n-1,对称"
10. 标在 **「补码定义」** 旁 → "正同原,负=反码末位+1"
11. 标在 **「补码范围」** 旁 → "-2^n ~ 2^n-1,多 1 负数"
12. 标在 **「补码位权法」** 旁 → "D=-x_{n-1}·2^{n-1}+Σx_i·2^i ⭐"
13. 标在 **「[-x]补」** 旁 → "连符号每位反,末位+1"
14. 标在 **「变形补码」** 旁 → "双符号位:00 正 11 负"
15. 标在 **「移码定义」** 旁 → "[x]移 = 2^n + x"
16. 标在 **「移码 vs 补码」** 旁 → "仅符号位相反 ⭐"
17. 标在 **「移码保序」** 旁 → "真值大→码大,可无符号比"
18. 标在 **「四码分工」** 旁 → "整→补、尾→原、阶→移"
19. 标在 **「C int 范围」** 旁 → "[-2^31, 2^31-1] 必背"
20. 标在 **「C 不同字长转」** 旁 → "先扩展,再转符号"

---

## 🔴 红笔标注(易错/陷阱)  共 11 条

1. 标在 **「原码 0」** 旁 → "⚠️ ±0 双表示,反码同样"
2. 标在 **「十进制小数」** 旁 → "⚠️ 0.3 永远转不准二进制"
3. 标在 **「分组法」** 旁 → "⚠️ 整左补 0、小右补 0"
4. 标在 **「8 位原码」** 旁 → "⚠️ 仅 255 个不同数(2.1题7)"
5. 标在 **「补码 -2^n」** 旁 → "⚠️ 原码无,反码无,只补/移有"
6. 标在 **「补码 1000...0」** 旁 → "⚠️ 8 位 = -128,无对应原码"
7. 标在 **「移码偏置」** 旁 → "⚠️ 偏置可能是 127 不是 128"
8. 标在 **「-x 移码」** 旁 → "⚠️ x=-2^n 时求 -x 移码溢出"
9. 标在 **「定点表示」** 旁 → "⚠️ 同位串可解释为整或小数"
10. 标在 **「C 混合运算」** 旁 → "⚠️ signed/unsigned 混 → 按无符号"
11. 标在 **「BCD 加法」** 旁 → "⚠️ 结果 ≥10 加 6 修正"

---

## ↔️ 跨章关联标注  共 5 条

1. 标在 **「补码减法」** 旁 → "→ 2.2 · 减法 = A + [-B]补"
2. 标在 **「变形补码」** 旁 → "→ 2.2 · 双符号位判溢出"
3. 标在 **「四码分工」** 旁 → "→ 2.3 · 尾数用原码 / 阶码用移码"
4. 标在 **「十进制小数」** 旁 → "→ 2.3 · 浮点舍入误差根源"
5. 标在 **「大小端」** 旁 → "→ 第 4 章 / CN / OS · 字节序"

---

**标注总数 = 20 蓝 + 11 红 + 5 跨章 = 36 条**
对应默写题 36 题(见同名答案版 / 空白版 PDF)。
