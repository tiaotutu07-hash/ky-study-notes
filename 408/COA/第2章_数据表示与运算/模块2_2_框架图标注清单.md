# 框架图标注清单 · 模块 2.2 运算方法和运算电路

> 蓝笔写知识点(≤15字),红笔写易错(≤20字),跨章关联另起。

---

## 🔵 蓝笔标注(知识点/规律)  共 25 条

1. 标在 **「全加器 FA」** 旁 → "三入两出 S+Cout"
2. 标在 **「FA 公式」** 旁 → "S=A⊕B⊕C, Cout=AB+(A⊕B)C"
3. 标在 **「串行 vs 并行」** 旁 → "串 O(n),并 O(log n)"
4. 标在 **「CLA 思想」** 旁 → "G,P 一次算所有 C_i"
5. 标在 **「OF」** 旁 → "OF=C_n⊕C_{n-1},判有符号"
6. 标在 **「CF」** 旁 → "CF=Sub⊕Cout,判无符号"
7. 标在 **「SF」** 旁 → "SF=最高位,有符号才有意义"
8. 标在 **「ZF」** 旁 → "结果为 0 时 ZF=1"
9. 标在 **「ALU 接口」** 旁 → "A,B 输入 + ALUop 控制"
10. 标在 **「逻辑移位」** 旁 → "空位双向补 0,视为无符号"
11. 标在 **「算术左移」** 旁 → "低位补 0,移出≠符号则溢出"
12. 标在 **「算术右移」** 旁 → "高位补符号位"
13. 标在 **「ROL/ROR」** 旁 → "不带 CF,首尾相接"
14. 标在 **「RCL/RCR」** 旁 → "带 CF,经过 CF 循环"
15. 标在 **「补码加减公式」** 旁 → "[A±B]补=[A]补+[±B]补"
16. 标在 **「补码运算特点」** 旁 → "符号位与数值位同算"
17. 标在 **「溢出法 1」** 旁 → "同号加→异号 = 溢出"
18. 标在 **「溢出法 2」** 旁 → "双符号:00正/01溢/10溢/11负"
19. 标在 **「溢出法 3」** 旁 → "C_n⊕C_{n-1}≠0 = 溢出"
20. 标在 **「加减电路」** 旁 → "Sub 控制反相,1 加器全包"
21. 标在 **「无符号比较」** 旁 → "看 ZF/CF,A<B→CF=1"
22. 标在 **「有符号比较」** 旁 → "看 OF⊕SF, ZF=0 即 A>B"
23. 标在 **「原码乘」** 旁 → "符号异或+绝对值,n次加+右移"
24. 标在 **「Booth 比较法」** 旁 → "y_n y_{n+1}: 00/11→0,01→+X,10→-X"
25. 标在 **「补码除法」** 旁 → "同号商1异号商0,末位恒置1"

---

## 🔴 红笔标注(易错/陷阱)  共 14 条

1. 标在 **「CF 减法时」** 旁 → "⚠️ 减法 CF=~Cout(借位)"
2. 标在 **「OF 适用」** 旁 → "⚠️ 无符号运算 OF 无意义"
3. 标在 **「CF 适用」** 旁 → "⚠️ 有符号运算 CF 无意义"
4. 标在 **「逻辑左移」** 旁 → "⚠️ 无符号 1 移出即溢出"
5. 标在 **「算术右移」** 旁 → "⚠️ 低位 1 移出 = 精度丢失"
6. 标在 **「补码丢高位」** 旁 → "⚠️ 模 2^(n+1),最高位进位丢弃"
7. 标在 **「同号才可能溢出」** 旁 → "⚠️ 异号加/同号减必不溢出"
8. 标在 **「双符号位」** 旁 → "⚠️ 真符号位看高位(第1位)"
9. 标在 **「加法器电路」** 旁 → "⚠️ 同电路无法识别有/无符号"
10. 标在 **「比较 SF」** 旁 → "⚠️ 溢出时 SF 翻转,需 OF 校正"
11. 标在 **「乘法运算」** 旁 → "⚠️ 右移用逻辑右移(数值位)"
12. 标在 **「乘法溢出 有符号」** 旁 → "⚠️ 高n位 ≠ 低n位符号 = 溢出"
13. 标在 **「除数为 0」** 旁 → "⚠️ 触发硬件异常 SIGFPE"
14. 标在 **「int 除法溢出」** 旁 → "⚠️ 仅 -2^31÷-1 唯一溢出"

---

## ↔️ 跨章关联标注  共 5 条

1. 标在 **「ALU 总成」** 旁 → "→ 第5章 · CPU 数据通路核心"
2. 标在 **「标志位 OF/CF/SF/ZF」** 旁 → "→ 第5章 · cmp/jcc 指令"
3. 标在 **「Booth 法/原码乘」** 旁 → "→ 2.1 · 补码定义/位权法"
4. 标在 **「除数为 0 异常」** 旁 → "→ 第5章 · CPU 异常处理"
5. 标在 **「位级等价性」** 旁 → "→ 2.1 · C 类型转换 / 2.3 · IEEE 编码"

---

**标注总数 = 25 蓝 + 14 红 + 5 跨章 = 44 条**
对应默写题 44 题。
