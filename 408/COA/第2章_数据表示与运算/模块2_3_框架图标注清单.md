# 框架图标注清单 · 模块 2.3 浮点数的表示与运算

> 蓝笔写知识点(≤15字),红笔写易错(≤20字),跨章关联另起。

---

## 🔵 蓝笔标注(知识点/规律)  共 25 条

1. 标在 **「浮点公式」** 旁 → "N=(-1)^S·M·R^E"
2. 标在 **「M 与 E 的分工」** 旁 → "M 决精度,E 决范围"
3. 标在 **「4 个不可表示区」** 旁 → "正负上溢/下溢"
4. 标在 **「规格化定义」** 旁 → "1/2≤|M|<1,基数=2"
5. 标在 **「原码规格化」** 旁 → "0.1xx/1.1xx"
6. 标在 **「补码规格化」** 旁 → "0.1xx/1.0xx"
7. 标在 **「左规」** 旁 → "尾左移阶减,可多次"
8. 标在 **「右规」** 旁 → "尾右移阶加,仅 1 次"
9. 标在 **「IEEE 754 单精度」** 旁 → "1+8+23,bias=127"
10. 标在 **「IEEE 754 双精度」** 旁 → "1+11+52,bias=1023"
11. 标在 **「偏置公式」** 旁 → "bias=2^(k-1)-1 ⭐"
12. 标在 **「隐藏位」** 旁 → "规格化最高 1 隐藏"
13. 标在 **「阶码全 0 尾全 0」** 旁 → "±0"
14. 标在 **「阶码全 0 尾非 0」** 旁 → "非规格化数,E=1-bias"
15. 标在 **「阶码全 1 尾全 0」** 旁 → "±∞"
16. 标在 **「阶码全 1 尾非 0」** 旁 → "NaN"
17. 标在 **「float 范围」** 旁 → "±3.4×10³⁸,有效6~7位"
18. 标在 **「double 范围」** 旁 → "±1.8×10³⁰⁸,有效15~16位"
19. 标在 **「加减五步」** 旁 → "对/加/规/舍/判"
20. 标在 **「对阶规则」** 旁 → "小阶向大阶看齐"
21. 标在 **「IEEE 默认舍入」** 旁 → "就近舍入到偶"
22. 标在 **「真溢出」** 旁 → "看阶码,不看尾数"
23. 标在 **「浮点乘除」** 旁 → "无需对阶,阶加减/尾乘除"
24. 标在 **「大端」** 旁 → "高字节→低地址,网络用"
25. 标在 **「struct 对齐两规则」** 旁 → "成员%其大小=0,总%最大=0"

---

## 🔴 红笔标注(易错/陷阱)  共 14 条

1. 标在 **「浮点上溢」** 旁 → "⚠️ 上溢必须中断"
2. 标在 **「浮点下溢」** 旁 → "⚠️ 下溢按机器零处理"
3. 标在 **「右规可能上溢」** 旁 → "⚠️ 阶 +1 可能超界"
4. 标在 **「左规可能下溢」** 旁 → "⚠️ 阶 -1 可能超界"
5. 标在 **「偏置公式」** 旁 → "⚠️ 是 2^(k-1)-1 不是 2^(k-1)"
6. 标在 **「非规格化阶码真值」** 旁 → "⚠️ E真=1-bias=-126"
7. 标在 **「IEEE 隐藏位」** 旁 → "⚠️ 仅规格化数有,非规格化无"
8. 标在 **「0.1 转二进制」** 旁 → "⚠️ 永不精确,必有舍入"
9. 标在 **「对阶」** 旁 → "⚠️ 大阶向小阶=高位丢失出错"
10. 标在 **「尾数溢出」** 旁 → "⚠️ 尾数溢出 ≠ 真溢出"
11. 标在 **「乘除规格化」** 旁 → "⚠️ 仅检查 1 次(已规格化)"
12. 标在 **「int→float」** 旁 → "⚠️ 可能丢精度(24位有效)"
13. 标在 **「float→int」** 旁 → "⚠️ 向 0 截断+可能溢出"
14. 标在 **「struct 顺序」** 旁 → "⚠️ 成员顺序影响 sizeof"

---

## ↔️ 跨章关联标注  共 4 条

1. 标在 **「浮点舍入误差」** 旁 → "→ 2.1 · 十进制小数转二进制不精"
2. 标在 **「IEEE 阶码用移码」** 旁 → "→ 2.1 · 移码保序性"
3. 标在 **「大小端 + 对齐」** 旁 → "→ 第4章 · 主存按字节寻址"
4. 标在 **「浮点运算」** 旁 → "→ 第5章 · 浮点指令 / 协处理器"

---

**标注总数 = 25 蓝 + 14 红 + 4 跨章 = 43 条**
对应默写题 43 题。
